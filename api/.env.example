# Google OAuth Tokens
GOOGLE_TOKEN_DIR=/home/ubuntu/.config/google

# Firestore (will auto-detect from Application Default Credentials or service account)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccountKey.json

# Project Keywords
CINDERELLA_KEYWORDS=cinderella,ncaa,college basketball,pe acquisition,wrexham

# API Config
PORT=3001
NODE_ENV=development
